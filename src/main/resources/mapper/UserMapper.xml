<mapper namespace="User">
    <select id="selectOne" paramterType="com.example.mybatis_xuexi.entity.User"
            resultType="com.example.mybatis_xuexi.entity.User">
        select * from user where id = #{id} and username= #{username};
    </select>
    <select id="selectList" resultType="com.example.mybatis_xuexi.entity.User">
        select * from user;
    </select>
</mapper>